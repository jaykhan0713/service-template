app:
  logging:
    mdc:
      headers:
        x-gateway-trace-id: gatewayTraceId
        x-user-id: userId
      method: "http.method"
      path: "http.path"
      status: "http.status"
      durationMs: "http.durationMs"